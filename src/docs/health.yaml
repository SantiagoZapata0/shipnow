paths:
  /api/health:
    get:
      tags:
        - Health
      summary: Estado de salud de la API
      description: Informa si el servidor y la conexión con MongoDB están disponibles.
      responses:
        "200":
          $ref: "#/components/GoodResponses/HealthResponse"
        "503":
          $ref: "#/components/BadResponses/ServiceUnavailableResponse"
