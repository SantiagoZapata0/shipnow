paths:
  /api/health:
    get:
      tags:
        - Health
      summary: Server health status
      description: Returns the health status of our server
      responses:
        "200":
          $ref: "#/components/GoodResponses/HealthResponse"
        