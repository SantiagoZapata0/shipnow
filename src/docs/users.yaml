paths:
  /api/users:
    get:
      tags:
        - Users
      summary: Get all users
      responses:
        "200":
          description: Una lista de usuarios
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/User"
          