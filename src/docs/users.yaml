paths:
  /api/users:
    get:
      tags:
        - Users
      summary: Methods to get users
      description: "All list of users"
      responses:
            "200":
              $ref: "#/components/responses/UserResponse"
          